<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.usjt.veiculop3" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="br.usjt.veiculop3.controller.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="br.usjt.veiculop3.controller.DetalheVeiculoActivity"
            android:label="@string/title_activity_detalhe_veiculo" >
        </activity>
        <activity
            android:name="br.usjt.veiculop3.controller.ListaVeiculoActivity"
            android:label="@string/title_activity_lista_veiculo" >
        </activity>
    </application>

</manifest>
